import 'package:flutter/material.dart';

class HomeVeiew extends StatelessWidget {
  const HomeVeiew({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Text("Hello"),
        ],
      ),
    );
  }
}
